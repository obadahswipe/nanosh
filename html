<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>For My Naya 💕</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Courier New', cursive;
            background: #ff6b9d;
            overflow-x: hidden;
            color: #333;
            position: relative;
        }

        body::before {
            content: '';
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(45deg, #ff6b9d, #feca57, #ff9ff3, #48dbfb, #ff6b9d);
            background-size: 400% 400%;
            animation: gradientShift 15s ease infinite;
            z-index: -1;
        }

        @keyframes gradientShift {
            0% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
            100% { background-position: 0% 50%; }
        }

        /* Hearts Background Animation */
        .heart-bg {
            position: fixed;
            font-size: 30px;
            animation: floatUp 15s infinite linear;
            z-index: 1;
            opacity: 0.3;
        }

        @keyframes floatUp {
            0% {
                transform: translateY(100vh) rotate(0deg);
                opacity: 0;
            }
            10% {
                opacity: 0.3;
            }
            90% {
                opacity: 0.3;
            }
            100% {
                transform: translateY(-100vh) rotate(360deg);
                opacity: 0;
            }
        }

        /* Loading Screen */
        .loader {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(135deg, #ff6b9d 0%, #ff9ff3 100%);
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            z-index: 10000;
            transition: opacity 1s ease;
        }

        .loader h1 {
            color: white;
            font-size: 1.8em;
            animation: heartbeat 1.5s infinite;
            text-align: center;
            padding: 20px;
        }

        .loader .hearts {
            font-size: 50px;
            animation: spin 2s infinite linear;
        }

        @keyframes heartbeat {
            0%, 100% { transform: scale(1); }
            50% { transform: scale(1.1); }
        }

        @keyframes spin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }

        /* Hero Section */
        .hero {
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            text-align: center;
            color: white;
            position: relative;
            padding: 20px;
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(10px);
        }

        .hero h1 {
            font-size: clamp(2em, 5vw, 3.5em);
            margin-bottom: 20px;
            text-shadow: 2px 2px 4px rgba(0,0,0,0.3);
            animation: glow 2s ease-in-out infinite alternate;
        }

        @keyframes glow {
            from { text-shadow: 0 0 10px #fff, 0 0 20px #ff6b9d, 0 0 30px #ff6b9d; }
            to { text-shadow: 0 0 20px #fff, 0 0 30px #ff9ff3, 0 0 40px #ff9ff3; }
        }

        .nickname {
            font-size: 1.5em;
            color: #fff;
            margin: 10px 0;
            font-style: italic;
            opacity: 0;
            animation: fadeIn 2s forwards;
            animation-delay: 1s;
        }

        .typewriter {
            font-size: clamp(1em, 3vw, 1.5em);
            margin: 20px;
            min-height: 40px;
            padding: 15px;
            background: rgba(255, 255, 255, 0.2);
            border-radius: 20px;
            backdrop-filter: blur(10px);
        }

        @keyframes fadeIn {
            to { opacity: 1; }
        }

        /* Animated Scroll Arrow */
        .scroll-arrow {
            position: absolute;
            bottom: 30px;
            left: 50%;
            transform: translateX(-50%);
            cursor: pointer;
            text-align: center;
        }

        .scroll-arrow span {
            display: block;
            width: 30px;
            height: 30px;
            border-bottom: 3px solid white;
            border-right: 3px solid white;
            transform: rotate(45deg);
            margin: -10px auto;
            animation: scrollBounce 2s infinite;
        }

        .scroll-arrow p {
            color: white;
            margin-top: 20px;
            font-size: 0.9em;
        }

        @keyframes scrollBounce {
            0%, 20%, 50%, 80%, 100% { transform: rotate(45deg) translateY(0); }
            40% { transform: rotate(45deg) translateY(-10px); }
            60% { transform: rotate(45deg) translateY(-5px); }
        }

        /* Countdown Section */
        .countdown-section {
            background: white;
            padding: 60px 20px;
            text-align: center;
            position: relative;
            overflow: hidden;
        }

        .countdown-section::before,
        .countdown-section::after {
            content: '💕';
            position: absolute;
            font-size: 100px;
            opacity: 0.1;
        }

        .countdown-section::before {
            top: -20px;
            left: -20px;
            transform: rotate(-15deg);
        }

        .countdown-section::after {
            bottom: -20px;
            right: -20px;
            transform: rotate(15deg);
        }

        .countdown-section h2 {
            color: #ff6b9d;
            font-size: clamp(1.8em, 4vw, 2.5em);
            margin-bottom: 10px;
        }

        .countdown-section .subtitle {
            color: #ff9ff3;
            font-size: 1.2em;
            margin-bottom: 30px;
            font-style: italic;
        }

        .countdown {
            display: flex;
            justify-content: center;
            gap: 15px;
            flex-wrap: wrap;
            margin-top: 30px;
        }

        .countdown-item {
            background: linear-gradient(135deg, #ff6b9d 0%, #ff9ff3 100%);
            color: white;
            padding: 15px;
            border-radius: 20px;
            min-width: 80px;
            box-shadow: 0 10px 30px rgba(255, 107, 157, 0.3);
            position: relative;
            overflow: hidden;
        }

        .countdown-item::before {
            content: '❤️';
            position: absolute;
            top: -10px;
            right: -10px;
            font-size: 30px;
            opacity: 0.2;
            transform: rotate(15deg);
        }

        .countdown-item .number {
            font-size: 2em;
            font-weight: bold;
        }

        .countdown-item .label {
            font-size: 0.8em;
            text-transform: uppercase;
            margin-top: 5px;
        }

        /* Memories Gallery */
        .gallery-section {
            background: rgba(255, 255, 255, 0.95);
            padding: 60px 20px;
            text-align: center;
            position: relative;
        }

        .gallery-section h2 {
            color: #ff6b9d;
            font-size: clamp(1.8em, 4vw, 2.5em);
            margin-bottom: 30px;
            position: relative;
        }

        .gallery-section h2::before,
        .gallery-section h2::after {
            content: '💖';
            position: absolute;
            top: 50%;
            transform: translateY(-50%);
        }

        .gallery-section h2::before {
            left: -40px;
        }

        .gallery-section h2::after {
            right: -40px;
        }

        .memories-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 25px;
            max-width: 1200px;
            margin: 0 auto;
        }

        .memory-card {
            background: linear-gradient(135deg, #fff 0%, #ffe0ec 100%);
            border-radius: 20px;
            padding: 25px;
            box-shadow: 0 10px 30px rgba(255, 107, 157, 0.2);
            transform: translateY(0);
            transition: all 0.3s;
            position: relative;
            overflow: hidden;
            cursor: pointer;
        }

        .memory-card::before {
            content: '';
            position: absolute;
            top: -50%;
            left: -50%;
            width: 200%;
            height: 200%;
            background: radial-gradient(circle, rgba(255, 107, 157, 0.1) 0%, transparent 70%);
            animation: pulse 3s infinite;
        }

        @keyframes pulse {
            0%, 100% { transform: scale(1); opacity: 0.5; }
            50% { transform: scale(1.1); opacity: 0.3; }
        }

        .memory-card:hover {
            transform: translateY(-10px) scale(1.02);
            box-shadow: 0 15px 40px rgba(255, 107, 157, 0.3);
        }

        .memory-card .memory-icon {
            font-size: 50px;
            margin-bottom: 15px;
        }

        .memory-card h3 {
            color: #ff6b9d;
            margin-bottom: 10px;
            font-size: 1.3em;
        }

        .memory-card p {
            color: #666;
            line-height: 1.6;
            font-size: 1em;
        }

        /* Love Notes Section */
        .love-notes-section {
            background: linear-gradient(135deg, #ff9ff3 0%, #ff6b9d 100%);
            padding: 60px 20px;
            text-align: center;
            position: relative;
            overflow: hidden;
        }

        .love-notes-section h2 {
            color: white;
            font-size: clamp(1.8em, 4vw, 2.5em);
            margin-bottom: 30px;
            text-shadow: 2px 2px 4px rgba(0,0,0,0.2);
        }

        .reasons-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 20px;
            max-width: 1000px;
            margin: 0 auto;
        }

        .reason-card {
            background: white;
            padding: 25px;
            border-radius: 20px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.1);
            transform: rotate(-2deg);
            transition: all 0.3s;
            position: relative;
        }

        .reason-card:nth-child(even) {
            transform: rotate(2deg);
        }

        .reason-card:hover {
            transform: rotate(0deg) scale(1.05);
            z-index: 10;
            box-shadow: 0 15px 40px rgba(0,0,0,0.2);
        }

        .reason-card::before {
            content: '💕';
            position: absolute;
            top: -15px;
            right: 10px;
            font-size: 30px;
            transform: rotate(15deg);
        }

        .reason-card h3 {
            color: #ff6b9d;
            margin-bottom: 15px;
            font-size: 1.3em;
        }

        .reason-card p {
            color: #666;
            line-height: 1.6;
        }

        /* Our Story Section */
        .story-section {
            background: white;
            padding: 60px 20px;
            text-align: center;
            position: relative;
        }

        .story-section h2 {
            color: #ff6b9d;
            font-size: clamp(1.8em, 4vw, 2.5em);
            margin-bottom: 30px;
        }

        .story-content {
            max-width: 800px;
            margin: 0 auto;
            padding: 40px;
            background: linear-gradient(135deg, #ffe0ec 0%, #fff 100%);
            border-radius: 30px;
            position: relative;
            box-shadow: 0 20px 60px rgba(255, 107, 157, 0.2);
        }

        .story-date {
            font-size: 2em;
            color: #ff6b9d;
            margin-bottom: 20px;
            font-weight: bold;
            position: relative;
            display: inline-block;
        }

        .story-date::before,
        .story-date::after {
            content: '✨';
            position: absolute;
            top: 50%;
            transform: translateY(-50%);
            animation: sparkle 2s infinite;
        }

        .story-date::before {
            left: -40px;
        }

        .story-date::after {
            right: -40px;
        }

        @keyframes sparkle {
            0%, 100% { opacity: 1; transform: translateY(-50%) scale(1); }
            50% { opacity: 0.5; transform: translateY(-50%) scale(1.2); }
        }

        .story-text {
            font-size: 1.2em;
            color: #666;
            line-height: 1.8;
            margin: 20px 0;
        }

        .infinity {
            font-size: 60px;
            color: #ff6b9d;
            animation: float 3s ease-in-out infinite;
        }

        @keyframes float {
            0%, 100% { transform: translateY(0); }
            50% { transform: translateY(-10px); }
        }

        /* Music Section */
        .music-section {
            background: linear-gradient(135deg, #ff6b9d 0%, #ff9ff3 100%);
            padding: 60px 20px;
            text-align: center;
            color: white;
            position: relative;
        }

        .music-section h2 {
            font-size: clamp(1.8em, 4vw, 2.5em);
            margin-bottom: 30px;
            text-shadow: 2px 2px 4px rgba(0,0,0,0.2);
        }

        .playlist-container {
            max-width: 600px;
            margin: 0 auto;
            background: rgba(255, 255, 255, 0.95);
            border-radius: 30px;
            padding: 30px;
            box-shadow: 0 20px 60px rgba(0,0,0,0.2);
        }

        .song-input-container {
            margin: 20px 0;
        }

        .song-input-container label {
            display: block;
            color: #ff6b9d;
            font-size: 1.2em;
            margin-bottom: 10px;
            font-weight: bold;
        }

        .song-input {
            width: 100%;
            padding: 15px;
            border: 3px solid #ff9ff3;
            border-radius: 15px;
            font-size: 1em;
            outline: none;
            transition: all 0.3s;
        }

        .song-input:focus {
            border-color: #ff6b9d;
            box-shadow: 0 0 10px rgba(255, 107, 157, 0.3);
        }

        .add-song-btn {
            background: linear-gradient(135deg, #ff6b9d 0%, #ff9ff3 100%);
            color: white;
            border: none;
            padding: 15px 30px;
            border-radius: 25px;
            font-size: 1.1em;
            cursor: pointer;
            margin-top: 15px;
            transition: all 0.3s;
            box-shadow: 0 5px 15px rgba(255, 107, 157, 0.3);
        }

        .add-song-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 20px rgba(255, 107, 157, 0.4);
        }

        .songs-list {
            margin-top: 30px;
            text-align: left;
        }

        .song-item {
            background: linear-gradient(135deg, #ffe0ec 0%, #fff 100%);
            padding: 15px;
            margin: 10px 0;
            border-radius: 15px;
            color: #666;
            display: flex;
            align-items: center;
            transition: all 0.3s;
        }

        .song-item:hover {
            transform: translateX(10px);
            box-shadow: 0 5px 15px rgba(255, 107, 157, 0.2);
        }

        .song-item::before {
            content: "🎵";
            font-size: 24px;
            margin-right: 15px;
        }

        /* Final Message */
        .message-section {
            background: white;
            padding: 60px 20px;
            text-align: center;
            position: relative;
        }

        .message-section h2 {
            color: #ff6b9d;
            font-size: clamp(1.8em, 4vw, 2.5em);
            margin-bottom: 30px;
        }

        .message-box {
            max-width: 700px;
            margin: 0 auto;
            background: linear-gradient(135deg, #ff6b9d 0%, #ff9ff3 100%);
            color: white;
            padding: 40px 30px;
            border-radius: 30px;
            box-shadow: 0 20px 60px rgba(255, 107, 157, 0.3);
            font-size: 1.1em;
            line-height: 1.8;
            position: relative;
            overflow: hidden;
        }

        .message-box::before {
            content: '💕';
            position: absolute;
            top: -30px;
            right: -30px;
            font-size: 100px;
            opacity: 0.2;
            transform: rotate(-15deg);
        }

        .message-box::after {
            content: '💖';
            position: absolute;
            bottom: -30px;
            left: -30px;
            font-size: 100px;
            opacity: 0.2;
            transform: rotate(15deg);
        }

        /* Floating Interactive Hearts */
        .floating-hearts-container {
            position: fixed;
            width: 100%;
            height: 100%;
            pointer-events: none;
            z-index: 999;
        }

        .floating-heart {
            position: absolute;
            font-size: 25px;
            cursor: pointer;
            pointer-events: all;
            animation: floatHeart 8s infinite ease-in-out;
            transition: transform 0.3s;
        }

        .floating-heart:hover {
            transform: scale(1.5);
        }

        @keyframes floatHeart {
            0%, 100% { transform: translateY(0) rotate(0deg); }
            25% { transform: translateY(-20px) rotate(-5deg); }
            75% { transform: translateY(10px) rotate(5deg); }
        }

        /* Responsive Design */
        @media (max-width: 768px) {
            .hero h1 { font-size: 2em; }
            .typewriter { font-size: 1em; }
            .gallery-section h2::before,
            .gallery-section h2::after { display: none; }
            .story-date::before,
            .story-date::after { display: none; }
            .memories-grid { grid-template-columns: 1fr; }
            .reasons-grid { grid-template-columns: 1fr; }
            .countdown-item { min-width: 70px; padding: 10px; }
            .countdown-item .number { font-size: 1.5em; }
        }

        /* Extra romantic touches */
        .sparkle {
            position: fixed;
            pointer-events: none;
            animation: sparkleAnim 1s linear forwards;
            z-index: 9999;
        }

        @keyframes sparkleAnim {
            0% {
                opacity: 1;
                transform: scale(0) rotate(0deg);
            }
            50% {
                opacity: 1;
                transform: scale(1) rotate(180deg);
            }
            100% {
                opacity: 0;
                transform: scale(0) rotate(360deg) translateY(-50px);
            }
        }
    </style>
</head>
<body>
    <!-- Background Hearts -->
    <div class="heart-bg" style="left: 10%; animation-delay: 0s;">❤️</div>
    <div class="heart-bg" style="left: 20%; animation-delay: 3s;">💕</div>
    <div class="heart-bg" style="left: 30%; animation-delay: 6s;">💖</div>
    <div class="heart-bg" style="left: 40%; animation-delay: 9s;">💗</div>
    <div class="heart-bg" style="left: 50%; animation-delay: 12s;">💝</div>
    <div class="heart-bg" style="left: 60%; animation-delay: 2s;">❤️</div>
    <div class="heart-bg" style="left: 70%; animation-delay: 5s;">💕</div>
    <div class="heart-bg" style="left: 80%; animation-delay: 8s;">💖</div>
    <div class="heart-bg" style="left: 90%; animation-delay: 11s;">💗</div>

    <!-- Loading Screen -->
    <div class="loader" id="loader">
        <div class="hearts">💕💖💕</div>
        <h1>Preparing something special for my Naya...</h1>
    </div>

    <!-- Floating Interactive Hearts -->
    <div class="floating-hearts-container">
        <span class="floating-heart" style="top: 10%; left: 10%; animation-delay: 0s;" onclick="showLoveMessage()">❤️</span>
        <span class="floating-heart" style="top: 20%; right: 15%; animation-delay: 1s;" onclick="showLoveMessage()">💕</span>
        <span class="floating-heart" style="top: 50%; left: 5%; animation-delay: 2s;" onclick="showLoveMessage()">💖</span>
        <span class="floating-heart" style="top: 70%; right: 10%; animation-delay: 3s;" onclick="showLoveMessage()">💗</span>
        <span class="floating-heart" style="top: 80%; left: 20%; animation-delay: 4s;" onclick="showLoveMessage()">💝</span>
        <span class="floating-heart" style="top: 30%; left: 50%; animation-delay: 5s;" onclick="showLoveMessage()">💘</span>
        <span class="floating-heart" style="top: 60%; right: 30%; animation-delay: 6s;" onclick="showLoveMessage()">💓</span>
    </div>

    <!-- Hero Section -->
    <section class="hero">
        <h1>For My Beautiful Naya 💕</h1>
        <p class="nickname">(My Sweet Nanosh) 💖</p>
        <div class="typewriter" id="typewriter"></div>
        <div class="scroll-arrow" onclick="scrollToNext()">
            <span></span>
            <p>Scroll for surprises</p>
        </div>
    </section>

    <!-- Countdown Section -->
    <section class="countdown-section" id="countdown-section">
        <h2>Counting Every Second Until Summer ☀️</h2>
        <p class="subtitle">When I can finally hold you again...</p>
        <div class="countdown" id="countdown">
            <div class="countdown-item">
                <div class="number" id="days">00</div>
                <div class="label">Days</div>
            </div>
            <div class="countdown-item">
                <div class="number" id="hours">00</div>
                <div class="label">Hours</div>
            </div>
            <div class="countdown-item">
                <div class="number" id="minutes">00</div>
                <div class="label">Minutes</div>
            </div>
            <div class="countdown-item">
                <div class="number" id="seconds">00</div>
                <div class="label">Seconds</div>
            </div>
        </div>
    </section>

    <!-- Memories Gallery -->
    <section class="gallery-section">
        <h2>Our Beautiful Memories Together</h2>
        <div class="memories-grid">
            <div class="memory-card" onclick="animateCard(this)">
                <div class="memory-icon">🇸🇾</div>
                <h3>The Last Day in Syria</h3>
                <p>That bittersweet moment when we had to say goodbye, not knowing it would lead to something even stronger. Every ending with you is just a new beginning.</p>
            </div>
            <div class="memory-card" onclick="animateCard(this)">
                <div class="memory-icon">🚌💝</div>
                <h3>The Necklace on the Bus</h3>
                <p>When I gave you that necklace in the back of the bus in Syria... The way your eyes lit up, I knew I wanted to see that smile forever.</p>
            </div>
            <div class="memory-card" onclick="animateCard(this)">
                <div class="memory-icon">🇯🇴💎</div>
                <h3>Our Jordan Moment</h3>
                <p>Giving you the necklace in Jordan, watching you wear it with that beautiful smile. Every time I see you with it, my heart skips a beat.</p>
            </div>
            <div class="memory-card" onclick="animateCard(this)">
                <div class="memory-icon">🌙✨</div>
                <h3>All Night on the Roof</h3>
                <p>That magical night we talked until sunrise on the roof in Syria. The stars were beautiful, but nothing compared to talking with you.</p>
            </div>
            <div class="memory-card" onclick="animateCard(this)">
                <div class="memory-icon">📱💕</div>
                <h3>Non-Stop Texting</h3>
                <p>When you went to Jordan and even being sick couldn't stop us from texting all day. Distance means nothing when someone means everything.</p>
            </div>
            <div class="memory-card" onclick="animateCard(this)">
                <div class="memory-icon">😂👣</div>
                <h3>The Feet Wake-Up Call</h3>
                <p>When I woke up with your feet in my face 😂 Even your feet are cute to me! That's how you know it's true love!</p>
            </div>
        </div>
    </section>

    <!-- Love Notes Section -->
    <section class="love-notes-section">
        <h2>Why You Changed Everything 💌</h2>
        <div class="reasons-grid">
            <div class="reason-card">
                <h3>You Opened My Heart</h3>
                <p>I was the most private person, keeping everything locked inside. But with you, Naya, all my walls came down. You didn't just enter my life, you became my life.</p>
            </div>
            <div class="reason-card">
                <h3>You're My Everything</h3>
                <p>I literally can't sleep without hearing your voice. Can't function without knowing you're okay. You've become as essential as breathing to me.</p>
            </div>
            <div class="reason-card">
                <h3>Your Beautiful Soul</h3>
                <p>The way you care, the way you love, the way you make everything better just by being you. Nanosh, you have the most beautiful soul I've ever known.</p>
            </div>
            <div class="reason-card">
                <h3>You Make Me Brave</h3>
                <p>With you, I'm not afraid to show my feelings, to be vulnerable, to love completely. You gave me courage I never knew I had.</p>
            </div>
            <div class="reason-card">
                <h3>Your Laugh is Magic</h3>
                <p>When you laugh, the whole world stops. It's the most beautiful sound, and I'd do anything just to hear it every day.</p>
            </div>
            <div class="reason-card">
                <h3>You're My Home</h3>
                <p>Doesn't matter where we are - Syria, Jordan, or anywhere else. As long as I'm with you, I'm home. You're my safe place, my peace, my everything.</p>
            </div>
        </div>
    </section>

    <!-- Our Story Section -->
    <section class="story-section">
        <h2>Our Love Story 💑</h2>
        <div class="story-content">
            <div class="story-date">11/07/2025</div>
            <p class="story-text">
                The day our story began... and it's still being written every single day. From that moment, everything changed. My world became brighter, my heart became fuller, and my life finally made sense.
            </p>
            <div class="infinity">∞</div>
            <p class="story-text">
                Our love story has no ending, only beautiful chapters that keep getting better...
            </p>
        </div>
    </section>

    <!-- Music Section -->
    <section class="music-section">
        <h2>Our Songs 🎵</h2>
        <div class="playlist-container">
            <div class="song-input-container">
                <label for="songInput">Naya, add your favorite songs here:</label>
                <input type="text" id="songInput" class="song-input" placeholder="Type a song name and press Add...">
                <button class="add-song-btn" onclick="addSong()">Add Song 💕</button>
            </div>
            <div class="songs-list" id="songsList">
                <div class="song-item">Waiting for you to add our first song...</div>
            </div>
        </div>
    </section>

    <!-- Final Message Section -->
    <section class="message-section">
        <h2>My Promise to You 💍</h2>
        <div class="message-box">
            <p><strong>My dearest Naya, my sweet Nanosh,</strong></p>
            <p>I was someone who kept everything inside, never letting anyone see the real me. Then you came along and changed everything. You didn't just break down my walls - you made me want to tear them down myself.</p>
            <p>Now I can't imagine a single day without you. I can't sleep without hearing your voice, can't function without knowing you're okay. You've become my heartbeat, my breath, my reason for everything.</p>
            <p>Every memory we've made - from that last day in Syria to waking up with your feet in my face (still makes me laugh!) - is treasured in my heart forever.</p>
            <p>This summer can't come fast enough. Until then, this website is my heart in digital form, a reminder that no distance can change what we have.</p>
            <p>I love you more than words can say, Naya. You're not just my girlfriend - you're my soulmate, my best friend, my everything.</p>
            <p>Forever and always yours,<br><strong>Obadah (Your Bobo) 💕</strong></p>
        </div>
    </section>

    <script>
        // Hide loader after page loads
        window.addEventListener('load', function() {
            setTimeout(() => {
                document.getElementById('loader').style.opacity = '0';
                setTimeout(() => {
                    document.getElementById('loader').style.display = 'none';
                }, 1000);
            }, 2000);
        });

        // Typewriter effect
        const messages = [
            "Naya, you are my everything 💕",
            "Distance means nothing when you mean everything 💖",
            "I can't wait to hold you again this summer ☀️",
            "You changed my whole world, Nanosh 💗",
            "Every second without you feels like forever 💝",
            "You're the reason I smile every day 😊",
            "My heart belongs to you forever 💘"
        ];

        let messageIndex = 0;
        let charIndex = 0;
        let currentMessage = '';
        let isDeleting = false;

        function typeWriter() {
            const typewriterElement = document.getElementById('typewriter');
            
            if (!isDeleting && charIndex < messages[messageIndex].length) {
                currentMessage += messages[messageIndex].charAt(charIndex);
                charIndex++;
            } else if (isDeleting && charIndex > 0) {
                currentMessage = currentMessage.substring(0, currentMessage.length - 1);
                charIndex--;
            } else if (!isDeleting && charIndex === messages[messageIndex].length) {
                isDeleting = true;
                setTimeout(typeWriter, 2000);
                return;
            } else if (isDeleting && charIndex === 0) {
                isDeleting = false;
                messageIndex = (messageIndex + 1) % messages.length;
            }
            
            typewriterElement.innerHTML = currentMessage;
            const speed = isDeleting ? 50 : 100;
            setTimeout(typeWriter, speed);
        }

        setTimeout(typeWriter, 3000);

        // Countdown Timer (Set to June 1st, 2025 for summer)
        function updateCountdown() {
            const summerDate = new Date('2025-06-01T00:00:00').getTime();
            const now = new Date().getTime();
            const distance = summerDate - now;

            const days = Math.floor(distance / (1000 * 60 * 60 * 24));
            const hours = Math.floor((distance % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60));
            const minutes = Math.floor((distance % (1000 * 60 * 60)) / (1000 * 60));
            const seconds = Math.floor((distance % (1000 * 60)) / 1000);

            document.getElementById('days').innerText = days.toString().padStart(2, '0');
            document.getElementById('hours').innerText = hours.toString().padStart(2, '0');
            document.getElementById('minutes').innerText = minutes.toString().padStart(2, '0');
            document.getElementById('seconds').innerText = seconds.toString().padStart(2, '0');

            if (distance < 0) {
                document.getElementById('countdown').innerHTML = "<h2>WE'RE FINALLY TOGETHER! 🎉💕🎊</h2>";
            }
        }

        setInterval(updateCountdown, 1000);
        updateCountdown();

        // Smooth scroll
        function scrollToNext() {
            document.getElementById('countdown-section').scrollIntoView({ behavior: 'smooth' });
        }

        // Love message popup
        const loveMessages = [
            "Naya, I love you to infinity! 💕",
            "You're my everything, Nanosh! 💖",
            "Can't wait for summer! ☀️",
            "Missing your smile right now! 😊",
            "You make my heart complete! 💗",
            "Forever isn't long enough with you! 💘",
            "You're my soulmate! 💝",
            "Thinking of you always! 💭",
            "You're perfect, my Naya! ✨"
        ];

        function showLoveMessage() {
            const message = loveMessages[Math.floor(Math.random() * loveMessages.length)];
            // Create a beautiful popup instead of alert
            const popup = document.createElement('div');
            popup.style.cssText = `
                position: fixed;
                top: 50%;
                left: 50%;
                transform: translate(-50%, -50%);
                background: linear-gradient(135deg, #ff6b9d, #ff9ff3);
                color: white;
                padding: 30px;
                border-radius: 20px;
                font-size: 1.5em;
                z-index: 10000;
                box-shadow: 0 20px 60px rgba(0,0,0,0.3);
                animation: popIn 0.5s ease;
                text-align: center;
            `;
            popup.innerHTML = message;
            document.body.appendChild(popup);
            
            setTimeout(() => {
                popup.style.animation = 'fadeOut 0.5s ease';
                setTimeout(() => popup.remove(), 500);
            }, 2000);
        }

        // Add animation styles
        const style = document.createElement('style');
        style.textContent = `
            @keyframes popIn {
                0% { transform: translate(-50%, -50%) scale(0); opacity: 0; }
                100% { transform: translate(-50%, -50%) scale(1); opacity: 1; }
            }
            @keyframes fadeOut {
                to { opacity: 0; transform: translate(-50%, -50%) scale(0.8); }
            }
        `;
        document.head.appendChild(style);

        // Animate memory cards
        function animateCard(card) {
            card.style.animation = 'bounce 0.5s ease';
            setTimeout(() => {
                card.style.animation = '';
            }, 500);
            
            // Add more hearts when clicked
            for(let i = 0; i < 5; i++) {
                setTimeout(() => createHeart(card), i * 100);
            }
        }

        function createHeart(element) {
            const heart = document.createElement('span');
            heart.innerHTML = '💕';
            heart.style.cssText = `
                position: absolute;
                top: 50%;
                left: 50%;
                transform: translate(-50%, -50%);
                font-size: 30px;
                pointer-events: none;
                animation: floatAway 1s ease forwards;
                z-index: 1000;
            `;
            element.appendChild(heart);
            setTimeout(() => heart.remove(), 1000);
        }

        // Add float away animation
        const floatStyle = document.createElement('style');
        floatStyle.textContent = `
            @keyframes floatAway {
                to {
                    transform: translate(-50%, -200%) scale(0);
                    opacity: 0;
                }
            }
            @keyframes bounce {
                0%, 100% { transform: scale(1); }
                50% { transform: scale(1.1); }
            }
        `;
        document.head.appendChild(floatStyle);

        // Song list functionality
        let songs = [];
        
        function addSong() {
            const input = document.getElementById('songInput');
            const songName = input.value.trim();
            
            if (songName) {
                songs.push(songName);
                updateSongsList();
                input.value = '';
                
                // Save to localStorage
                localStorage.setItem('ourSongs', JSON.stringify(songs));
                
                // Show success message
                showLoveMessage();
            }
        }

        function updateSongsList() {
            const songsList = document.getElementById('songsList');
            if (songs.length > 0) {
                songsList.innerHTML = songs.map(song => 
                    `<div class="song-item">${song}</div>`
                ).join('');
            }
        }

        // Load saved songs
        window.addEventListener('load', function() {
            const savedSongs = localStorage.getItem('ourSongs');
            if (savedSongs) {
                songs = JSON.parse(savedSongs);
                updateSongsList();
            }
        });

        // Allow Enter key to add song
        document.getElementById('songInput').addEventListener('keypress', function(e) {
            if (e.key === 'Enter') {
                addSong();
            }
        });

        // Add sparkles on mouse move (romantic effect)
        let lastSparkle = 0;
        document.addEventListener('mousemove', (e) => {
            const now = Date.now();
            if (now - lastSparkle > 100) {
                if (Math.random() > 0.9) {
                    const sparkle = document.createElement('div');
                    sparkle.innerHTML = '✨';
                    sparkle.className = 'sparkle';
                    sparkle.style.left = e.clientX + 'px';
                    sparkle.style.top = e.clientY + 'px';
                    sparkle.style.fontSize = Math.random() * 20 + 10 + 'px';
                    document.body.appendChild(sparkle);
                    setTimeout(() => sparkle.remove(), 1000);
                    lastSparkle = now;
                }
            }
        });

        // Create floating hearts periodically
        setInterval(() => {
            const heart = document.createElement('div');
            heart.innerHTML = ['❤️', '💕', '💖', '💗', '💝'][Math.floor(Math.random() * 5)];
            heart.className = 'heart-bg';
            heart.style.left = Math.random() * 100 + '%';
            heart.style.fontSize = Math.random() * 20 + 20 + 'px';
            heart.style.animationDuration = Math.random() * 5 + 10 + 's';
            document.body.appendChild(heart);
            setTimeout(() => heart.remove(), 15000);
        }, 3000);
    </script>
</body>
</html>
